from django import forms
from .models import *

class TipForm(forms.ModelForm):
    class Meta:
        model = Tip
        fields = '__all__'

class HackForm(forms.ModelForm):
    class Meta:
        model = Hack
        fields = '__all__'
